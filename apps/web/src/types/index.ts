/**
 * Type re-exports from @oh-my-class/schemas.
 * Components import from here, not directly from schemas package.
 */
export type {
	Artifact,
	ArtifactContent,
	LessonPlan,
	Run,
} from "@oh-my-class/schemas";
