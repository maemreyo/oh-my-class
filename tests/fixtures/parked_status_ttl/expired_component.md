Status: Parked
Parked-Until: 2000-01-01

This fixture is intentionally expired so `tests/test_parked_status_ttl.py` can
prove the policy detects stale parked components without failing the live repo scan.
