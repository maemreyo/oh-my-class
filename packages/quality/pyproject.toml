[project]
name = "packages.quality"
version = "0.1.0"
description = "6-layer quality gate system for oh-my-class"
requires-python = ">=3.12"
dependencies = [
    "common.contracts",
    "pydantic>=2.0.0",
    "pyyaml>=6.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["."]
