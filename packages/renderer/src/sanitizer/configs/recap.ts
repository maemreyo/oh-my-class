import type { IOptions } from "sanitize-html";
import { BASE_CONFIG } from "../base-config.js";

// Recap is a static display — concept cards with no interactive elements
export const RECAP_CONFIG: IOptions = BASE_CONFIG;

export default RECAP_CONFIG;
