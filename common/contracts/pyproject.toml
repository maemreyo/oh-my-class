[project]
name = "common.contracts"
version = "0.1.0"
description = "Pydantic models shared between agents and quality gates"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["."]
