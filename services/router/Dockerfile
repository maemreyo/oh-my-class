FROM node:22-slim
WORKDIR /app
# TODO: Replace with actual 9Router image when available
# For now, placeholder
RUN echo "9Router sidecar placeholder"
EXPOSE 20128
CMD ["echo", "9Router — configure with real image"]
